[pytest]
markers =
    integration: tests that need a real Ollama instance running on the host
; Integration tests are skipped by default; run them with: pytest -m integration
addopts = -m "not integration"
asyncio_mode = auto
